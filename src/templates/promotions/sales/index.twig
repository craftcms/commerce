{% extends "commerce/_layouts/promotions" %}
{% set selectedSidebarNavItem = 'sales' %}


{% block content %}
    <h2>{{ "Sales"|t }}</h2>

    <div id="no-sales"{% if sales|length %} class="hidden"{% endif %}>
        <p>{{ "No sales exist yet."|t }}</p>
    </div>

    {% if sales|length %}
    <table id="sales" class="data fullwidth collapsible">
        <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Duration</th>
            <th scope="col">Discount Amount</th>
            <td class="thin"></td>
        </tr>
        </thead>
        <tbody>
        {% for sale in sales %}
            <tr data-id="{{ sale.id }}" data-name="{{ sale.name }}">
                <th scope="row" data-title="Name">
                    {% if sale.enabled %}
                        <div class="status enabled"></div>
                    {% else %}
                        <div class="status"></div>
                    {% endif %}
                    <a href="{{ url('commerce/promotions/sales/'~sale.id) }}">{{ sale.name }}</a>
                </th>
                <td data-title="Dates">
                    {{ sale.dateFrom ? sale.dateFrom.localeDate() : '∞'|t }}
                    -
                    {{ sale.dateTo ? sale.dateTo.localeDate() : '∞' }}
                </td>
                <td data-title="Discount Amount">
                    {% if sale.discountType == 'percent' %}
                        {{ sale.discountAmountAsPercent }} {{ "off"|t }}
                    {% else %}
                        {{ sale.discountAmountAsFlat|commerceCurrency }} {{ "off"|t }}
                    {% endif %}
                </td>
                <td class="thin"><a class="delete icon" title="{{ 'Delete'|t }}" role="button"></a></td>
            </tr>
        {% endfor %}

        </tbody>
    </table>
    {% endif %}

    <div class="buttons secondary-buttons">
        <a href="{{ url('commerce/promotions/sales/new') }}" class="btn submit add icon">New sale</a>
    </div>
{% endblock %}


{% includejs %}
var adminTable = new Craft.AdminTable({
tableSelector: '#sales',
noObjectsSelector: '#no-sales',
deleteAction: 'commerce/sales/delete'
});
{% endincludejs %}
