{% include '_includes/forms/componentSelect.twig' with {
    options: options ?? craft.commerce.shippingCategories.getAllShippingCategories(storeId ?? null),
    createAction: (create ?? true) ? 'commerce/shipping-categories/edit' : null,
    createParams: createParams ?? (storeHandle is defined ? {storeHandle: storeHandle} : {}),
} %}
