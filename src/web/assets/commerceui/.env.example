DEV_SERVER_PORT="8085"
DEV_SERVER_PUBLIC_PATH="http://localhost:$DEV_SERVER_PORT/"
# Until we have admintable as a package or everything in a mono repo we need this
# to be able to directly include components from the craftcms/cms repo.
CRAFT_ASSETS_PATH='./../../../../../cms/src/web/assets/'