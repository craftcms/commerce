{% extends "commerce/_layouts/settings" %}

{% set selectedTab = 'settings' %}
{% import "_includes/forms" as forms %}


{% block content %}
<h2>{{ "Order Fields"|t }}</h2>

<form method="post" accept-charset="UTF-8" data-saveshortcut>
    <input type="hidden" name="action" value="commerce/orderSettings/save">
    <input type="hidden" name="redirect" value="commerce/settings/general">

    {{ getCsrfInput() }}
    {% if orderSettings.id %}<input type="hidden" name="orderSettingsId" value="{{ orderSettings.id }}">{% endif %}

    {% include "_includes/fieldlayoutdesigner" with {
    fieldLayout: orderSettings.getFieldLayout(),
    } only %}

    <div class="buttons">
        <input type="submit" class="btn submit" value="{{ 'Save'|t }}">
    </div>
</form>
{% endblock %}
